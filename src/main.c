#include "../inc/main.h"

int main(int argc, char const* argv[])
{
	hashTable = ht_create(MAX_VERTEX_COUNT * 2);
	graph = graph_create();

	FILE* fp = getFileFromUser();
	int parseFile(fp)


	return 0;
}
